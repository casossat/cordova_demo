import { FormatoNombre.PipePipe } from './formato-nombre.pipe.pipe';

describe('FormatoNombre.PipePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoNombre.PipePipe();
    expect(pipe).toBeTruthy();
  });
});
